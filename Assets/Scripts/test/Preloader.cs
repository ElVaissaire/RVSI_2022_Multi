/*using Unity.Engine;
using Unity.Engine.UI;
using Unity.Netcode;

public class Preloader : MonoBehaviour
{
    [SerializedField] private Transform mlapiButtonContainer;
    [SerializedField] private Transform gameAccountButtonContainer;
    [SerializedField] private Transform launchButtonContainer;

    private void Awake()
    {

    }
    
    //
    
    private void OnNetworkManagerStart()
    {
        foreach (Transform t in launchButtonContainer)
            t.GetComponent<Button>().interactable = true;
    }


}*/

